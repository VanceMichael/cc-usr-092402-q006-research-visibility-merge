{
  "name": "visibility-ledger",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "build": "for f in src/*.js test/*.js; do node --check \"$f\" || exit 1; done"
  },
  "dependencies": {
    "fastify": "latest",
    "better-sqlite3": "latest"
  }
}
